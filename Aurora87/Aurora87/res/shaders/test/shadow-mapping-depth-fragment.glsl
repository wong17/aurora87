#version 460 core

void main()
{
    // Nada que hacer. Solo escribimos profundidad por defecto.
    // El valor de profundidad se calcula automaticamente por el framebuffer de profundidad.
    // gl_FragDepth = gl_FragCoord.z;
}